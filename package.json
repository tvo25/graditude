{
  "name": "frontend",
  "description": "Graditude",
  "version": "0.1.0",
  "engines": {
    "node": "10.16.0"
  },
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install && npm run build"
  }
}
